export enum StorageKey {
  FLOATING_WIDGET_POSITION = 'floating-widget-position',
  USER_ID = 'userId',
  DEVICE_ID = 'deviceId',
  FAVORITE_PROMPTS = 'favoritePrompts',
  OPTIONS = 'options',
}

export enum StorageArea {
  SYNC = 'sync',
  LOCAL = 'local',
}
